---
import SiteLayout from './SiteLayout.astro';
import type { PageMeta } from '../content/contracts';

const { meta } = Astro.props as { meta: PageMeta };
---

<SiteLayout meta={meta}>
  <div class="page--wide">
    <slot />
  </div>
</SiteLayout>
